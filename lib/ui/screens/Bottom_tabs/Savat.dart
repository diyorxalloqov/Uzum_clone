
import 'package:flutter/material.dart';

class Savat extends StatelessWidget {
  const Savat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Savat"),);
  }
}