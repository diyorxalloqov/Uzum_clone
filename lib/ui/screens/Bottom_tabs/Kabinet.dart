import 'package:flutter/material.dart';

class Kabinet extends StatelessWidget {
  const Kabinet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Kabinet"),);
  }
}