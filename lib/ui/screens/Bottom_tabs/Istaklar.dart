import 'package:flutter/material.dart';

class Istaklar extends StatelessWidget {
  const Istaklar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Istaklar"));
  }
}